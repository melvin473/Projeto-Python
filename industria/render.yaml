services:
  - type: web
    name: plataforma-preditiva
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn seu_projeto_raiz.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0